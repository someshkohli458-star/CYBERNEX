'use client'
import {useState} from 'react'
const defaults={sound:true,animations:true,scanlines:true,compact:false}
export default function Settings(){const [s,setS]=useState(defaults); const toggle=(k:keyof typeof s)=>setS({...s,[k]:!s[k]}); return <main className="shell"><div className="container"><header className="topbar"><a className="back" href="/">←</a><div className="brand">SETTINGS</div><div className="status">SYSTEM</div></header><section className="hero"><div className="eyebrow">[ INTERFACE CONFIGURATION ]</div><h1>CYBERNEX SETTINGS</h1><p>Customize the safe simulation experience on this device.</p></section><section className="settings">{Object.entries(s).map(([k,v])=><button className="settingRow" key={k} onClick={()=>toggle(k as keyof typeof s)}><span><b>{k.replace(/^./,x=>x.toUpperCase())}</b><small>{k==='sound'?'Interface feedback':k==='animations'?'Motion effects':k==='scanlines'?'Terminal visual effect':'Dense layout mode'}</small></span><strong className={v?'on':'off'}>{v?'ON':'OFF'}</strong></button>)}</section><section className="panel"><div className="section-title">SAFETY MODE</div><p>CYBERNEX simulations never connect to cameras, accounts, networks or other real-world targets.</p></section><div className="buttons"><a className="btn" href="/profile">PROFILE</a><a className="btn secondary" href="/admin">ADMIN PANEL</a></div></div></main>}
